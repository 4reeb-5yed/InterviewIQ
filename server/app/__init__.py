"""InterviewIQ backend application package.

This package is the root of the FastAPI backend. Modules (config, core,
agents, features, db, middleware, utils) are added in subsequent Phase 1
tasks per docs/TASKS.md.
"""

__version__ = "0.1.0"
